# Tells all web crawlers that they are allowed to crawl the site.
User-agent: *

# Allows crawlers to access all content.
Allow: /

# Disallows crawlers from accessing the API configuration file.
Disallow: /api_config.js

# Specifies the location of the sitemap.
Sitemap: https://tikip.us/sitemap.xml

